// Connection settings for the Sales GP Calculator.
// endpoint: Apps Script web app URL that ends in /exec
// token: must match SHARED_TOKEN in apps-script/Code.gs
// Leave the PASTE_ placeholders below and the app runs in local browser mode.
// Each user can then point it at the sheet once via the Connect Sheets button.
// Full notes: assets/config.example.js
window.GP_CONFIG = { endpoint: 'PASTE_YOUR_EXEC_URL_HERE', token: 'PASTE_YOUR_TOKEN_HERE' };
